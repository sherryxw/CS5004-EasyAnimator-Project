package cs5004.animator.model;

/**
 * This enum represents the animation types.
 * Move: relocate the shape position
 * Scale: reshape the width and height of the shape
 * Color: change the RGB color of the shape
 */
public enum AnimationsType {
  MOVE, COLOR, SCALE;
}
